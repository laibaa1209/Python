
#the val inputted is always a string, example being the two lines of code down below
val = input("enter any value: ")
print(type(val))   #always strings

#but if you want the values to fetch a certain datatype, then you can simply type cast it, example being the two line of code down below

val = int(input("enter any value: "))
print(type(val)) #since we have type casted it to int, the output will be int

